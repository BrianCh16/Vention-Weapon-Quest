{
  "name": "weapon-quest",
  "version": "1.0.0",
  "description": "Take-home assessment.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "migrate": "npx knex migrate:latest",
    "rollback": "npx knex migrate:rollback --all",
    "seed": "npx knex seed:run",
    "tmigrate": "npx knex migrate:latest --env test",
    "trollback": "npx knex migrate:rollback --all --env test",
    "tseed": "npx knex seed:run --env test",
    "test": "jest --verbose"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^11.0.0",
    "express": "^4.17.2",
    "knex": "^3.1.0",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
